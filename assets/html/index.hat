<p>
  <a href="/new">new</> " | "
  <a href="/recent">recent</> " | "
  <a href="/jump">jump

<h1> deadwiki
<article#index-page>
  if empty?(pages)
    <i> "This page will either list your wiki pages or display your `index.md` (if it exists)."
  else
    header := ""
    <ul> for page in pages
      if nested?(page.title)
        if header != nested_header(page.title)
          if header != "" then print("</details>")
          header = nested_header(page.title)
          print("<details> {<summary> header}")
          <li> <a href={page.url}> nested_title(page.title)
        else
          <li> <a href={page.url}> page.title
      else
        if header != ""
          print("</details>")
          header = ""
        <li> <a href={page.url}> page.title
